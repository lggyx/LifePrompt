/**
 * capture/constants.js — zero-dep constants for the capture module.
 * Safe to import statically anywhere — no Node.js dependencies.
 */

export const CAPTURE_TYPE = {
  TEXT: 'text',
  LINK: 'link',
  IMAGE: 'image',
};
